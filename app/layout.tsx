import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'ChannelHub - Hotel Channel Management',
  description: 'Professional hotel channel management system for managing rates, availability, and bookings across multiple distribution channels.',
  keywords: 'hotel management, channel manager, booking management, rate management, hotel software',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}